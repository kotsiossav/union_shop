import 'package:flutter/material.dart';

class AppStyles {
  // Big, bold title
  static const TextStyle title = TextStyle(
    fontSize: 24,
    fontWeight: FontWeight.bold,
  );

  // Slightly smaller, still bold subtitle
  static const TextStyle subtitle = TextStyle(
    fontSize: 18,
    fontWeight: FontWeight.bold,
  );
}
