import 'package:flutter/material.dart';

// Centralized text styles for consistent typography across the app
class AppStyles {
  // Large title style used for main page headings and hero sections
  // 24px size with bold weight for maximum impact
  static const TextStyle title = TextStyle(
    fontSize: 24,
    fontWeight: FontWeight.bold,
  );

  // Medium subtitle style used for section headings and subheadings
  // 18px size with bold weight to maintain hierarchy
  static const TextStyle subtitle = TextStyle(
    fontSize: 18,
    fontWeight: FontWeight.bold,
  );
}
